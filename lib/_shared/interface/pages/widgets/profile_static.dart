import 'package:flutter/material.dart';

class ProfileStatic extends StatelessWidget {
  const ProfileStatic({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Card(
      child: Text("linechart"),
    );
  }
}
